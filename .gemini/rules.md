# Antigravity Agent Rules

CRITICAL INSTRUCTION FOR ALL AI AGENTS WORKING ON THIS REPOSITORY:

- **NEVER RUN `npm run build` OR ANY BUILD COMMAND IN THIS PROJECT.**
- Never attempt to build or run build scripts automatically.
